/**
 * 每个页面都存在的dom元素，如：每个页面都存在组件 backToTop
 */

const backToTopContainer = document.querySelector(".backToTop__container"); // 回到顶部组件

const topAnchor = document.querySelector("#top-anchor");

export { backToTopContainer, topAnchor };
